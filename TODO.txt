
- May crash when clicking on an alert whereas we did not finished the execution of the request first.
Should either push the click into a queue, or block the click until the request is finished.

- The TreeModel does not always update children/parent checkstates (see in alerts creation's dialog).
See TreeModel.py, function setData() of TreeeModel.

- When closing, shutdown all the treadpools.

- Add documentation of methods.

- Create style for results comparison in AlertWidget to highlight them.
